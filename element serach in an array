#include<iostream>
using namespace std;
int main()
{
    int n,num,count=0,i;
    cin>>n;
    int arr[n];
    for(i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    cin>>num;
    for(i=0;i<n;i++)
    {
        if(arr[i]==num)
        {
            count=count+1;
        }
    }
    if(count)
      cout<<"True";
    else
       cout<<"False";
     return 0;
}
