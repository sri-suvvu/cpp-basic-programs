#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,num,i,count=0;
    cin>>n>>num;
    int arr[n];
    for(i=0;i<n;i++)
      cin>>arr[i];
    for(i=0;i<n;i++){
    if(arr[i]%num!=0)
     continue;
    else
      count++;
    }
    cout<<count;
    
    
}
