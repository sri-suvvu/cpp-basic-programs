#include<bits/stdc++.h>
using namespace std;
int main()
{
    int num,i,sum=0,n,m,count=0;
    cin>>num;
    int arr[num];
    for(i=0;i<num;i++)
    {
        cin>>arr[i];
    }
    cin>>n>>m;
    for(i=0;i<num;i++)
    {
        if(arr[i]>=n&&arr[i]<=m)
        {
        continue;
		 }
         else
         {
        cout<<arr[i]<<" ";
        count++;
         }
    }
    if(count==0)
    cout<<"-1";
    return 0;
}
